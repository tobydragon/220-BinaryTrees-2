package edu.ithaca.dragon.datastructures.tree;

public class BTNodeUtil {

    //the number of edges on the longest path in the tree
    public static int height(BTNode root){
        return -1;
    }

    // add a new node in the tree, such that the tree stays roughly balanced
    // if the height of the left subtree is greater than the right, add somewhere right
    // else add somewhere left
    public static void add(BTNode root, int newKey){

    }
    
}
